# Project name. Override per deployment (COMPOSE_PROJECT_NAME in .env, or -p)
# when running several stacks on one host — it namespaces containers, networks
# and volumes, so each stack gets its own database and chain data.
name: ${COMPOSE_PROJECT_NAME:-dashscan}

services:
  # Opt-in only: starts with `--profile local-db`. Production leaves this
  # profile off and points DATABASE_URL at an external postgres.
  postgres:
    image: postgres:17-alpine
    profiles: ["local-db"]
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-dashscan}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dashscan}
      POSTGRES_DB: ${POSTGRES_DB:-dashscan}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dashscan} -d ${POSTGRES_DB:-dashscan}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # Opt-in only: starts with `--profile local-dashcore`. Production leaves this
  # profile off and points CORE_RPC_HOST at an external node.
  #
  # The RPC port is deliberately not published to the host: -rpcallowip is open
  # so api/indexer can reach it, so it must stay on the compose network only.
  # A full mainnet chain needs ~40GB+ in the dashd_data volume.
  dashd:
    image: dashpay/dashd:23.1.8
    profiles: ["local-dashcore"]
    restart: unless-stopped
    volumes:
      - dashd_data:/home/dash
    ports:
      # P2P only, so the node can accept inbound peers.
      - "${CORE_P2P_PORT:-9999}:${CORE_P2P_PORT:-9999}"
    command:
      - dashd
      - -printtoconsole
      - -chain=${CORE_CHAIN:-main}
      - -server=1
      - -listen=1
      - -rpcbind=0.0.0.0
      - -rpcallowip=0.0.0.0/0
      - -rpcport=${CORE_RPC_PORT:?CORE_RPC_PORT must be set}
      - -rpcuser=${CORE_RPC_USER:?CORE_RPC_USER must be set}
      - -rpcpassword=${CORE_RPC_PASSWORD:?CORE_RPC_PASSWORD must be set}
      - -port=${CORE_P2P_PORT:-9999}
      # The indexer subscribes to all five of these on one endpoint.
      - -zmqpubhashblock=tcp://0.0.0.0:${CORE_ZMQ_PORT:-29998}
      - -zmqpubrawtx=tcp://0.0.0.0:${CORE_ZMQ_PORT:-29998}
      - -zmqpubrawtxlock=tcp://0.0.0.0:${CORE_ZMQ_PORT:-29998}
      - -zmqpubrawtxlocksig=tcp://0.0.0.0:${CORE_ZMQ_PORT:-29998}
      - -zmqpubrawchainlocksig=tcp://0.0.0.0:${CORE_ZMQ_PORT:-29998}
    healthcheck:
      test:
        - CMD
        - dash-cli
        - -rpcconnect=127.0.0.1
        - -rpcport=${CORE_RPC_PORT:-9998}
        - -rpcuser=${CORE_RPC_USER:-dashmate}
        - -rpcpassword=${CORE_RPC_PASSWORD:-dashmate}
        - getblockchaininfo
      interval: 15s
      timeout: 10s
      retries: 10
      # Loading the block index on an existing chain can take several minutes.
      start_period: 300s

  # Opt-in only: starts with `--profile local-redis`. Production leaves this
  # profile off and points REDIS_URL at an external instance.
  redis:
    image: redis:8-alpine
    profiles: ["local-redis"]
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  # One-shot schema migration. The indexer only applies migrations via its
  # `migrate` subcommand (which exits when done) — a normal indexer start does
  # NOT migrate — so this runs first and everything touching the DB waits for
  # it to exit 0. Refinery records applied migrations in refinery_schema_history,
  # so re-running on every `up` is a no-op once the schema is current.
  migrate:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-pshenmic/dashscan}-indexer:${VERSION:-nightly}
    # `build` is for local development; production pulls the tag above.
    build:
      context: ./indexer
    command: ["migrate"]
    # Must not be `unless-stopped`: this container is meant to exit.
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
        required: false
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must be set}
      # Config::from_env() demands the full set even for a migration run.
      REDIS_URL: ${REDIS_URL:?REDIS_URL must be set}
      NETWORK: ${NETWORK:-mainnet}
      CORE_RPC_HOST: ${CORE_RPC_HOST}
      CORE_RPC_PORT: ${CORE_RPC_PORT}
      CORE_RPC_USER: ${CORE_RPC_USER}
      CORE_RPC_PASSWORD: ${CORE_RPC_PASSWORD}
      CORE_ZMQ_URL: ${CORE_ZMQ_URL}
      CORE_P2P_HOST: ${CORE_P2P_HOST}
      CORE_P2P_PORT: ${CORE_P2P_PORT}

  indexer:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-pshenmic/dashscan}-indexer:${VERSION:-nightly}
    build:
      context: ./indexer
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      # required: false lets this reference survive when the local-db profile
      # is off; when it is on, startup still waits for postgres to be healthy.
      postgres:
        condition: service_healthy
        required: false
      dashd:
        condition: service_healthy
        required: false
      redis:
        condition: service_healthy
        required: false
    # Lets CORE_RPC_HOST=host.docker.internal reach a Dash Core node
    # running on the host (e.g. dashmate).
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must be set}
      REDIS_URL: ${REDIS_URL:?REDIS_URL must be set}
      NETWORK: ${NETWORK:-mainnet}
      CORE_RPC_HOST: ${CORE_RPC_HOST}
      CORE_RPC_PORT: ${CORE_RPC_PORT}
      CORE_RPC_USER: ${CORE_RPC_USER}
      CORE_RPC_PASSWORD: ${CORE_RPC_PASSWORD}
      CORE_ZMQ_URL: ${CORE_ZMQ_URL}
      CORE_P2P_HOST: ${CORE_P2P_HOST}
      CORE_P2P_PORT: ${CORE_P2P_PORT}
      START_HEIGHT: ${START_HEIGHT:-0}

  api:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-pshenmic/dashscan}-api:${VERSION:-nightly}
    build:
      context: ./api
    restart: unless-stopped
    depends_on:
      # The API queries indexer-owned tables at startup, so it waits too.
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
        required: false
      dashd:
        condition: service_healthy
        required: false
      redis:
        condition: service_healthy
        required: false
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must be set}
      REDIS_URL: ${REDIS_URL:?REDIS_URL must be set}
      NETWORK: ${NETWORK:-mainnet}
      CORE_RPC_HOST: ${CORE_RPC_HOST}
      CORE_RPC_PORT: ${CORE_RPC_PORT}
      CORE_RPC_USER: ${CORE_RPC_USER}
      CORE_RPC_PASSWORD: ${CORE_RPC_PASSWORD}
    # No `ports:` here on purpose — a fixed host port cannot be published by
    # more than one replica. haproxy fronts these and publishes API_PORT.
    expose:
      - "3005"
    deploy:
      replicas: ${API_REPLICAS:-4}

  # Load balancer in front of the api replicas. This is what actually listens
  # on API_PORT; it discovers replicas via Docker's DNS, so `docker compose up
  # -d --scale api=N` needs no haproxy restart (keep N <= the server-template
  # count in haproxy.cfg).
  haproxy:
    image: haproxy:3.1-alpine
    restart: unless-stopped
    depends_on:
      - api
    # Config is inlined at the bottom of this file (top-level `configs:`), so
    # docker-compose.yml + .env are the only files a deploy host needs.
    configs:
      - source: haproxy_cfg
        target: /usr/local/etc/haproxy/haproxy.cfg
    ports
      # Binds to 127.0.0.1 by default so a host-level reverse proxy (system
      # haproxy/nginx) fronts it and the API is not exposed directly. Set
      # BIND_ADDR=0.0.0.0 to publish on all interfaces. When running several
      # stacks on one host, give each a unique API_PORT.
      - "${BIND_ADDR:-127.0.0.1}:${API_PORT:-3005}:3005"
    healthcheck:
      test: ["CMD-SHELL", "haproxy -c -f /usr/local/etc/haproxy/haproxy.cfg || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  # Opt-in only: starts with `--profile local-frontend`. Leave the profile off
  # when the frontend is served from somewhere else (CDN, static host, separate
  # deploy) and this stack only provides the API.
  # NOTE: unlike api/indexer, CI does not publish a frontend image, so this is
  # built locally rather than pulled by tag. DASHSCAN_API_URL is baked into the
  # bundle at build time, so a published image would also have to be rebuilt per
  # environment rather than pulled by version.
  frontend:
    profiles: ["local-frontend"]
    image: dashscan-frontend:${VERSION:-local}
    build:
      context: ./frontend
      # Baked into the bundle at build time — changing these needs a rebuild,
      # not just a restart.
      args:
        DASHSCAN_API_URL: ${DASHSCAN_API_URL}
        NETWORK: ${NETWORK:-mainnet}
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${FRONTEND_PORT:-3000}:3000"

volumes:
  postgres_data:
  redis_data:
  dashd_data:

# Inlined so this file is self-contained — nothing to copy to the deploy host.
configs:
  haproxy_cfg:
    content: |
      global
          log stdout format raw local0 info
          maxconn 8192

      defaults
          log     global
          mode    http
          option  httplog
          option  dontlognull
          option  forwardfor
          # Retry on another backend if one dies mid-request.
          option  redispatch
          retries 3
          timeout connect 5s
          timeout client  60s
          timeout server  60s
          timeout http-request 15s

      # Docker's embedded DNS. Compose gives every api replica the same `api` DNS
      # name, so resolving it repeatedly is what discovers containers as they come
      # and go — no restart needed when replicas are rescaled.
      resolvers docker
          nameserver dns 127.0.0.11:53
          resolve_retries 3
          timeout resolve 1s
          timeout retry   1s
          hold valid   10s
          hold other   10s
          hold refused 10s
          hold nx      10s
          hold timeout 10s

      frontend api_front
          bind *:3005
          default_backend api_back

      backend api_back
          balance roundrobin
          # No health checks by design: every replica stays in rotation and
          # whatever it answers (including 503 while syncing) is passed straight
          # through. Hard failures are still covered by `option redispatch` +
          # `retries` in defaults — a refused connection is retried on the next
          # server rather than being returned to the client.
          #
          # server-template pre-creates N slots resolved from the `api` DNS name.
          # Keep this >= API_REPLICAS; spare slots sit idle until they resolve.
          server-template api 8 api:3005 resolvers docker init-addr none

      # Stats/monitoring UI. Not published to the host — reach it with
      # `docker compose exec haproxy wget -qO- http://127.0.0.1:8404/`.
      frontend stats
          bind *:8404
          stats enable
          stats uri /
          stats refresh 10s
